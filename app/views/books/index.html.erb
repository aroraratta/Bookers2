<%= render partial: 'users/userinfo', locals: { user: current_user, book: @book } %>

<h2>Books</h2>

<table>
  <tr>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  <% @books.each do |book| %>
    <tr>
      <th><%= book.title %></th>
      <th><%= book.body %></th>
    </tr>
  <% end %>
</table>